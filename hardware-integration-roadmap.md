# 硬件接入与模型训练路线

## 一、最小硬件组合

- WiFi CSI：2 块 ESP32/ESP32-S3，或其他支持 CSI 采集的 WiFi 设备。
- 按压检测：微动开关，或压力传感器 + HX711。
- 区域停留：PIR 红外、门磁、红外对射或毫米波存在传感器。
- 边缘网关：电脑、树莓派或小型工控机。

## 二、第一阶段：设备连通

目标：先不训练模型，只确认每个设备能稳定上报。

1. CSI 采集端持续输出时间戳、子载波幅度、相位或可用特征。
2. 按压传感器输出时间戳、按压开始、按压结束、按压强度。
3. 停留传感器输出时间戳、进入区域、离开区域。
4. 边缘网关把所有数据统一保存为 CSV 或 SQLite。

## 三、第二阶段：标注采集

建议先采集 5 类动作：

- 直接经过。
- 靠近但未手消。
- 按压手消后揉搓。
- 整理袖口或戴手套。
- 多人干扰场景。

每类至少采集 30-50 段，每段保留 10-30 秒。

## 四、第三阶段：模型训练

推荐先做轻量模型：

- 基线：规则引擎，按压事件 + 停留时间 + CSI 能量变化。
- 第一版模型：1D-CNN 或 CNN-LSTM。
- 后续增强：Transformer 或自监督预训练 + 少量标注微调。

输出类别建议保持克制：

- 疑似发生手卫生。
- 未形成疑似手卫生。
- 无法判断，进入人工复核。

## 五、第四阶段：接入小程序

边缘网关提供两个接口：

- `/health`：检查网关是否在线。
- `/status`：返回设备状态、最新事件、复核队列。

小程序只负责展示和提醒，不直接采集 CSI 原始数据。

## 六、临床试点边界

- 不使用摄像头。
- 不输出个人最终合规结论。
- 多人干扰、证据不足、传感器异常统一进入人工复核。
- 若引入工牌/RFID，应做去标识化和授权管理。
